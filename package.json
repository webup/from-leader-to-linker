{
  "name": "from-leader-to-linker",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "rimraf dist && slidev build",
    "export": "slidev export --dark --timeout 0 --output ./from-leader-to-linker.pdf",
    "serve": "npm run build && netlify dev"
  },
  "dependencies": {
    "@slidev/cli": "^0.40.16",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*"
  },
  "devDependencies": {
    "@iconify-json/ri": "^1.1.8",
    "netlify-cli": "^14.4.0",
    "playwright-chromium": "^1.33.0",
    "rimraf": "^5.0.0"
  }
}
