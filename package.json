{
  "name": "from-leader-to-linker",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "rimraf dist && slidev build",
    "export": "slidev export --dark --timeout 0 --output ./from-leader-to-linker.pdf",
    "serve": "netlify dev"
  },
  "dependencies": {
    "@slidev/cli": "^0.37.1",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*"
  },
  "devDependencies": {
    "@iconify-json/ri": "^1.1.4",
    "netlify-cli": "^12.2.9",
    "playwright-chromium": "^1.28.1",
    "rimraf": "^3.0.2"
  }
}
